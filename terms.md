# Terms and Vocab

- relational database: that stores the data in table, and that table consists of rows and columns.
- database integrity : is the assurance that the data stored in a database is accurate, complete, consistent and reliable throughout it's lifecycle.
- schema: named collection of tables is called a schema.
